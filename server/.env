# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/heartheal

# JWT secret for authentication (use a stronger key in production)
JWT_SECRET=supersecretkey

# Server port (optional, defaults to 3000 if not provided)
PORT=10000
